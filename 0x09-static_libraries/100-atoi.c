#include "main.h"

/**
 * _atoi - converts a string to an integer
 * @s: the string to convert
 *
 * Return: the integer value of the string, or 0 if no digits are found
 */
int _atoi(char *s)
{
(void) s;
return (0);
}

